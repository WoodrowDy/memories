---
title: 작성 규칙 (CONVENTIONS)
created: 2026-05-29
updated: 2026-07-28
tags: [meta]
---

# 작성 규칙

GitHub에서 읽히는 위키이자, 나중에 옵시디언 Vault로 그대로 쓰기 위한 규칙.
핵심 원칙: **폴더는 가볍게, 분류는 태그·링크로, 파일은 옮기지 않는다.**

이 문서는 **파일을 어디에 두는가**를 정한다. 본문을 **어떻게 쓰는가**는
[노트 작성 스타일](docs/note-style.md)에서 따로 정한다.

## 두 개의 축

- **흐름**: `daily`(날것 TIL) → `topics`(정제된 위키). 익을수록 `status`가 올라간다.
- **주제**: os / db / lang / infra / cs / ai / tools. 폴더로 느슨하게 묶기만 한다.

## 어느 카테고리에 넣는가

경계가 애매할 때는 이 한 가지를 묻는다.

**"회사를 옮겨도 그대로 쓰는 지식인가?"**

- 그렇다 → `cs`. 개념·프로토콜·패턴·알고리즘처럼 제품에 매이지 않는 것.
- 아니다 → 그 제품이 속한 카테고리. 특정 벤더·제품·언어를 알아야 성립하는 사용법과 운영.

실제로 갈린 예가 위키에 이미 있다. `프로세스 vs 스레드`는 OS가 제공하는 실행 단위를
다루니 `os`에 있고, `동시성 vs 병렬성`은 실행 단위와 무관한 개념이라 `cs`에 있다.
`gRPC`는 특정 회사 제품이 아니라 프로토콜과 계약 설계라서 `cs`다.

두 곳에 걸치면 **본문의 무게중심**으로 정하고, 파일은 한 곳에만 둔다.
다른 쪽은 태그로 잇는다 — gRPC 노트가 `topics/cs/`에 있으면서
`tags: [cs/grpc, cs/network, cs/architecture]`를 다는 게 그 방식이다.

각 카테고리의 범위는 [topics/README.md](topics/README.md)의 표에 적혀 있다.

## 새 카테고리는 언제 만드는가

기본값은 **만들지 않는 것**이다. 폴더가 늘어나면 "그거 어디다 뒀더라"가 다시 시작되고,
그건 이 위키가 피하려던 바로 그 비용이다. 아래 셋을 **모두** 만족할 때만 만든다.

1. 기존 카테고리 어디에도 억지 없이는 들어가지 않는다
2. 앞으로 **최소 세 편**은 나올 주제다 — 한 편짜리 카테고리는 만들지 않는다
3. 기존 카테고리의 하위 개념이 아니다. 하위면 그 안에서 폴더로 쪼갠다
   (`topics/db/postgresql/`처럼)

하나라도 어긋나면 가장 가까운 카테고리에 넣고, 그 카테고리 README의 `작성 예정`에
주제만 적어둔다. 세 편이 쌓이면 그때 승격한다. 파일을 옮기는 비용은 링크 고치는 값이고,
그건 세 편이 모인 뒤에 한 번 치르는 게 싸다.

만들기로 했으면 같은 커밋에서 셋을 함께 한다.

1. `topics/<이름>/README.md` — MOC 생성, 첫 줄에 범위를 적는다
2. `topics/README.md`의 표에 한 행 추가
3. 태그 네임스페이스를 `<이름>/...`으로 시작

`memories-wiki-bot`은 새 카테고리를 **직접 만들지 않는다.** 위 기준으로 판단해 PR에
담아 제안할 뿐이고, 실제로 만들지는 사람이 머지로 정한다.

## 프론트매터 (모든 노트 맨 위)

```yaml
---
title: PostgreSQL 인덱스
aliases: [PG 인덱스, postgres index]   # 다른 이름으로도 검색·링크
created: 2026-05-29
updated: 2026-05-29
tags: [db/postgresql]
status: seedling
---
```

- `status`: `seedling`(씨앗) → `growing`(자라는 중) → `evergreen`(위키로 굳음) → `archived`(끝/관심 밖)
- `archived`로 바꿔도 **파일은 옮기지 않는다.** 상태 변경은 한 줄 수정으로 끝.
- `aliases`: 한/영 혼용 검색용. 필요 없으면 비워둬도 됨.

## 네이밍

- 파일·폴더는 **영문 kebab-case** (`my-note.md`). 공백·한글 파일명 금지(URL·링크 안정).
- **한 개념 = 한 파일.** 커지면 쪼갠다 (`postgresql.md` → `postgresql/indexing.md`).
- 파일명에 날짜·draft 같은 휘발성 단어 금지 (위키 페이지는 경로가 고정 ID).
- 예외: `daily`는 `YYYY-MM-DD.md`.

## 링크

- **표준 마크다운 링크** 사용: `[인덱스](indexing.md)`. GitHub·옵시디언 둘 다 동작.
- `[[위키링크]]`는 GitHub에서 깨지므로, 옵시디언 전용이 되기 전까지 쓰지 않는다.
- 각 페이지 하단에 **`## 관련 문서`** 섹션으로 손수 연결(백링크 대체 + 큐레이션).

## daily → topics 승격

1. 배운 건 `daily/<연도>/<날짜>.md`에 빠르게 적는다.
2. 건질 내용은 `topics/<주제>/`에 새 페이지로 정제한다(daily 내용을 복사·다듬기).
3. daily 노트에서 그 topic 페이지로 링크. daily 파일은 저널로 그 자리에 남긴다.

## MOC (폴더마다 README.md)

- 각 폴더의 `README.md`가 그 영역의 **지도**다 (GitHub 자동 렌더 + 옵시디언 MOC).
- 익은 페이지부터 큐레이션해 링크한다. 모든 파일을 다 나열할 필요는 없다.

## PARA는 여기서만

- `projects/`(끝 있는 목표)와 `personal/`(언어 등 Area)의 **인덱스 페이지에만** 가볍게 적용.
- dev 지식(`topics`)은 PARA를 쓰지 않는다 — 주제로만 둔다.
- 즉 PARA는 "폴더 트리"가 아니라 "인덱스 몇 개의 상태 관리"로만 쓴다.
